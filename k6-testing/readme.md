K6 must be installed on your system to run these tests. If you have K6 installed, just ensure the system is running and if it is, choose the test you would like to run. The three commands are:
```
npm run stress
npm run spike
npm run soak
```